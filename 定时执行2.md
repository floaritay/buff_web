# 定时自动执行脚本

脚本需要 Cookie 才能运行。Cookie 保存在 `cookie.txt`，可以直接创建文件并粘贴。
也可以先运行：

---

```bash
python item_buyer.py 12345 --max-price 1.0
# 或直接运行start_buy.bat
# 提示 "请输入 Cookie:" 时粘贴，自动保存到 cookie.txt
# 之后所有脚本共用，无需重复输入
```

## 轮询 vs 定时执行

| 模式 | 命令 | 适用场景 |
|------|------|----------|
| **单次运行** | `python item_buyer.py 12345 --max-price 5.0` | 配合任务计划程序定时调用 |
| **轮询模式** | `python item_buyer.py 12345 --max-price 5.0 --interval 30` | 程序自己循环，不需要任务计划 |

**轮询模式不需要再配定时任务**，程序会自己每隔 N 秒检查一次。适合短期盯盘，手动 Ctrl+C 停止。

**单次运行 + 任务计划** 适合长期无人值守，由系统定时触发。

---

## BAT 脚本参数说明

### `start_buy.bat`（交互式）

双击运行，按提示输入：

```
Enter item URL or goods_id:     → 饰品 URL 或 ID（必填）
Max price in CNY (default 1.0): → 价格阈值，直接回车默认 1.0 元
Max buy count (default 5):      → 最多买几个，直接回车默认 5
Select mode (1/2):              → 1=单次运行  2=轮询模式
Polling interval in seconds:    → 轮询间隔（选 2 后出现）
Max rounds, 0=infinite:         → 最大轮次，0=无限（选 2 后出现）
```

### 手动创建的 BAT（见下方示例）

下方示例 BAT 中 `python item_buyer.py` 后面的参数可以按需修改：

| 参数 | 说明 | 默认值 | 示例 |
|------|------|--------|------|
| `12345` | goods_id 或商品页面 URL | 必填 | `https://buff.163.com/goods/12345` |
| `--max-price 5.0` | 价格阈值（元），低于此价格才买 | 1.0 | `--max-price 0.5` |
| `--max-items 3` | 单轮最大购买数量 | 5 | `--max-items 10` |
| `--interval 30` | 轮询间隔（秒），不传则单次运行 | 不轮询 | `--interval 60` |
| `--max-rounds 20` | 最大轮次，0=无限，配合 `--interval` | 0 | `--max-rounds 50` |

---

## Windows：任务计划程序

### 1. 创建 BAT 脚本

以下示例按需选用，保存为 `.bat` 文件，修改 goods_id 和参数。

**指定饰品购买（单次）**

```bat
@echo off
chcp 65001 >nul 2>&1
set PYTHONIOENCODING=utf-8
cd /d "%~dp0"
python item_buyer.py 12345 --max-price 5.0 --max-items 3
timeout /t 3 >nul
```

**指定饰品购买（轮询，每 30 秒检查一次）**

```bat
@echo off
chcp 65001 >nul 2>&1
set PYTHONIOENCODING=utf-8
cd /d "%~dp0"
python item_buyer.py 12345 --max-price 5.0 --interval 30
timeout /t 3 >nul
```

> 轮询模式 BAT 直接双击运行即可，不需要配任务计划程序。

**涂鸦购买**

```bat
@echo off
chcp 65001 >nul 2>&1
set PYTHONIOENCODING=utf-8
cd /d "%~dp0"
python buff_buyer.py
timeout /t 3 >nul
```

**挂件搜枪**

```bat
@echo off
chcp 65001 >nul 2>&1
set PYTHONIOENCODING=utf-8
cd /d "%~dp0"
python buff_charm_searcher_austin.py
timeout /t 3 >nul
```

### 2. 设置定时任务（仅单次运行需要）

1. 打开任务计划程序（`Win+S` 搜索"任务计划程序"，或运行 `taskschd.msc`）
2. 点击 **"创建任务"**（注意不是"创建基本任务"）
3. **常规**：填写任务名称（如 `buff_item_buyer`）
4. **触发器** → 新建 → 高级设置中勾选"重复任务间隔"，选择间隔时间（如 5 分钟），持续时间选"无期限"
5. **操作** → 新建 → 启动程序 → 浏览选择 `.bat` 文件
6. **条件**：勾选"唤醒计算机以运行此任务"
7. 点击确定保存

---

## Android：Termux + crontab

### 1. 配置环境（首次）

```bash
pkg update && pkg upgrade -y
pkg install python -y
pip install requests
termux-setup-storage
```

### 2. 传文件到手机

将整个项目文件夹传到手机 Download 目录，Termux 中路径为 `~/storage/downloads/Buff`。

### 3. 预存 Cookie

```bash
cd ~/storage/downloads/Buff
python item_buyer.py 12345 --max-price 1.0
# 输入 Cookie，看到保存成功后 Ctrl+C 退出
```

### 4. 创建 Shell 脚本

**指定饰品购买**

创建 `run_item_buyer.sh`：

```bash
#!/bin/bash
cd ~/storage/downloads/Buff
python item_buyer.py 12345 --max-price 5.0 --max-items 3
```

**涂鸦购买**

创建 `run_buff_buyer.sh`：

```bash
#!/bin/bash
cd ~/storage/downloads/Buff
python buff_buyer.py
```

**挂件搜枪**

创建 `run_charm_searcher.sh`：

```bash
#!/bin/bash
cd ~/storage/downloads/Buff
python buff_charm_searcher_austin.py
```

赋权：

```bash
chmod +x run_item_buyer.sh run_buff_buyer.sh run_charm_searcher.sh
```

### 5. 安装 crontab

```bash
pkg install cronie -y
crond start
echo "crond start" >> ~/.bashrc
source ~/.bashrc
```

### 6. 添加定时任务（仅单次运行需要）

```bash
crontab -e
```

在编辑器末尾添加（`*/5` 表示每 5 分钟）：

```bash
# 指定饰品购买，每 5 分钟
*/5 * * * * python ~/storage/downloads/Buff/item_buyer.py 12345 --max-price 5.0 --max-items 3

# 涂鸦购买，每 5 分钟
*/5 * * * * python ~/storage/downloads/Buff/buff_buyer.py

# 挂件搜枪，每 10 分钟
*/10 * * * * python ~/storage/downloads/Buff/buff_charm_searcher_austin.py
```

保存：`Ctrl+O` → 回车 → `Ctrl+X`。

> 如果用轮询模式（`--interval 30`），直接在终端前台运行即可，不需要 crontab。

### 7. 验证

```bash
crontab -l        # 查看已设任务
crontab -e        # 编辑定时任务
crontab -r        # 删除所有定时任务
```

### 常用 Cron 表达式

| 表达式 | 含义 |
|--------|------|
| `*/5 * * * *` | 每 5 分钟 |
| `*/10 * * * *` | 每 10 分钟 |
| `0 * * * *` | 每小时整点 |
| `0 9 * * *` | 每天 9:00 |
| `0 */2 * * *` | 每 2 小时 |
| `30 8 * * 1-5` | 工作日 8:30 |

格式：`分 时 日 月 周`
